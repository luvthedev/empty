{
  "name": "static-site",
  "version": "1.0.0",
  "description": "Static site generator project built with Eleventy",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "deploy": "bash deploy.sh",
    "deploy:build": "bash deploy.sh build"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
